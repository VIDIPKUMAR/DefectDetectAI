# Create light requirements file
cat > requirements-light.txt << 'EOF'
# Lightweight requirements - no compilation needed
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
# OpenCV headless (no GUI dependencies)
opencv-python-headless==4.8.1.78
# NumPy - use compatible version for Mac ARM
numpy==1.24.3
pillow==10.1.0
redis==5.0.1
prometheus-client==0.19.0
pytest==7.4.3
pytest-asyncio==0.21.1
EOF